import SwiftUI

struct ContentView: View {
    var body: some View {
        NavigationView {
            List {
                Text("control_next")
                Text("control_next")
                Text("control_next")
            }
        }
    }
}
